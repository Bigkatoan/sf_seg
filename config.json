{
  "data_root": "data",
  "epochs": 100,
  "batch_size": 64,
  "lr": 0.0001,
  "num_workers": 8,
  "log_dir": "logs",
  "output_dir": "outputs",
  "checkpoint_dir": "checkpoints",
  "batch_accumulate": 1, 
  "loss_type": "combine",
  "image_size": 128,
  "num_channels": 64
}
